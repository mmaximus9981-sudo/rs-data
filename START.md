# 시작하기 — 처음부터 끝까지

압축을 풀면 파일이 여럿 나오는데, **직접 만질 파일은 셋뿐이다.**

| 파일 | 언제 |
|---|---|
| `rs_app_colab.ipynb` | 데이터를 만들 때 (코랩에서 연다) |
| `app.html` | 결과를 볼 때 (더블클릭) |
| `PUBLISH.md` | 폰에서도 보고 싶을 때 |

나머지는 위 셋이 불러 쓰는 부품이다. 열어볼 일 없다.

---

## 1. 드라이브에 올리기

압축을 풀고, 나온 파일 **전부**를 구글 드라이브에 올린다.

1. [drive.google.com](https://drive.google.com) → 내 드라이브
2. 우클릭 → **새 폴더** → 이름 `RS_APP`
3. 폴더에 들어가서 압축 푼 파일들을 통째로 끌어다 놓는다

`android` 폴더가 같이 올라가도 상관없다. 코랩은 무시하고, 나중에 앱을 만들 때 쓴다.

## 2. 코랩에서 열기

드라이브의 `RS_APP` 폴더에서 **`rs_app_colab.ipynb` 를 더블클릭.**
코랩이 열린다. (`.py` 파일은 더블클릭해도 안 열린다 — 노트북만 열린다)

## 3. 셀 순서대로 실행

노트북 왼쪽의 ▶ 버튼을 위에서부터 누른다.

| 셀 | 하는 일 | 걸리는 시간 |
|---|---|---|
| 1. 준비 | 드라이브 연결, 패키지 설치 | 1분 |
| 2. 빠른 확인 | 60종목으로 시험 | 3~5분 |
| 3. 전체 실행 | 500종목 | 20~40분 |
| 4. 결과 보기 | 오늘 뭐가 걸렸나 | 즉시 |
| 5. 앱 미리보기 | 노트북 안에서 앱 확인 | 즉시 |

셀 1에서 드라이브 권한 팝업이 뜨면 허용한다.
`필요한 파일: 전부 있음` 이 나오면 제대로 된 것이다.

**셀 2에서 이 줄을 꼭 본다.**

```
[스크린] 유니버스 60 · RS>=80 N · 패턴 적중 M
```

`RS>=80` 이 10 안팎이면 정상이다. 1~2개뿐이면 뭔가 잘못됐으니 노트북 맨 아래
문제 해결을 본다. 정상이면 셀 3으로 넘어간다.

## 4. 결과 보기

셀 5를 실행하면 노트북 안에서 앱이 그대로 뜬다. 카드를 누르면 상세가 열린다.

PC에서 크게 보려면 드라이브의 `app.html` 을 다운로드해서 더블클릭한다.
(드라이브 웹에서 바로 열면 미리보기라 작동하지 않는다)

---

## 여기까지가 오늘 할 일이다.

폰에서 보고 데이터를 자동으로 갱신하는 건 그다음 단계고, `PUBLISH.md` 에 있다.
먼저 결과가 쓸 만한지부터 보는 게 순서다.

---

## 파일 설명 (궁금할 때만)

| 파일 | 역할 |
|---|---|
| `run_sp500.py` | 파이프라인 전체를 엮는 진입점. **테마 목록이 여기 있다** |
| `fetch_data.py` | 야후에서 데이터를 받아오는 부분. 나중에 유료 공급자로 갈아끼울 곳 |
| `screener_live.py` | RS·트렌드템플릿·패턴 5종·지지선 계산 |
| `snapshot_export.py` | 파워플레이·U/D볼륨·어닝반응 계산 + JSON 만들기 |
| `app_template.html` | 앱 원본. **화면을 고칠 땐 여기를 고친다** |
| `app.html` | 데이터가 들어간 실행본. 자동 생성되니 직접 고치지 말 것 |
| `latest.json` | 스냅샷. 실행할 때마다 새로 만들어진다 |
| `publish_github.py` | 데이터를 인터넷에 올리는 도구 (2단계에서 씀) |
| `test_pipeline.py` | 인터넷 없이 로직만 시험 |
| `make_demo.py` | 가짜 데이터 생성기 (지금 `app.html` 에 든 게 이것) |
| `android/` | 폰 앱 프로젝트 |

## 지금 `app.html` 에 든 데이터는 가짜다

NVDX, ARCL 같은 종목은 실존하지 않는다. 화면을 확인하라고 만든 데모다.
셀 3을 돌리면 진짜 S&P500 데이터로 덮어씌워진다.
