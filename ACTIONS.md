# 매일 아침 자동 갱신 — GitHub Actions

목표: **내 PC도 코랩도 켜지 않는다.** 깃허브 서버가 매일 아침 스크리너를 돌리고,
결과를 인터넷에 올리고, 폰의 앱이 그걸 받아간다.

무료다. 이 작업은 하루 30분쯤 쓰는데 무료 한도는 월 2,000분이다.

---

## 전체 그림

```
매일 22:00 UTC (한국 아침 7시)
        │
   깃허브 서버가 run_sp500.py 실행
        │
   validate_snapshot.py 로 검증  ──✗──> 배포 안 함 (어제 데이터 유지)
        │ ✓
   data 브랜치에 latest.json 강제 푸시
        │
   https://내아이디.github.io/rs-data/latest.json
        │
      폰의 앱
```

**검증 단계가 핵심이다.** 사람이 안 보는 파이프라인에서 제일 위험한 건 실패가 아니라
'조용히 망가진 결과가 배포되는 것'이다. 야후가 절반만 응답해도 스크리너는 성공으로
끝나고 종목 3개짜리 스냅샷이 폰까지 간다. 그래서 배포 직전에 막는다.

**data 브랜치를 따로 쓰는 이유**는 저장소 크기다. 매일 몇 MB짜리 JSON을 main 에 쌓으면
1년이면 수 GB가 된다. data 브랜치는 매번 이력 없이 통째로 덮어써서 커밋이 항상 하나다.

---

## 1단계 · 코드를 저장소에 올리기

지금까지는 `latest.json` 만 올렸다. 이제 **코드 전체**가 올라가야 한다.
깃허브 서버가 그 코드를 실행할 거니까.

### Claude Code 를 쓴다면

`RS_APP` 폴더에서 Claude Code 를 켜고 이렇게 말하면 된다.

> 이 폴더를 mmaximus9981-sudo/rs-data 저장소의 main 브랜치에 올려줘

### 웹에서 직접 한다면

저장소 → **Add file** → **Upload files** → 폴더 내용을 끌어다 놓고 **Commit changes**

> ⚠️ 웹 업로드는 `.github` 폴더를 빠뜨리기 쉽다. 점(.)으로 시작하는 폴더는
> 윈도우 탐색기에서 숨겨져 있을 수 있으니, 보기 → 숨긴 항목 을 켜고 확인할 것.
> `.github/workflows/daily-snapshot.yml` 이 저장소에 보여야 한다.

## 2단계 · Actions 켜기

저장소 → **Actions** 탭 → 노란 배너가 보이면
**I understand my workflows, go ahead and enable them** 클릭.

왼쪽에 **일일 스냅샷** 이 보이면 워크플로가 인식된 것이다.

## 3단계 · 손으로 한 번 돌려보기

예약을 기다리지 말고 지금 시험한다.

**Actions** → 왼쪽 **일일 스냅샷** → 오른쪽 **Run workflow** →

| 항목 | 값 |
|---|---|
| 종목 수 제한 | `60` ← 첫 시험은 작게 |
| 실적 수집 건너뛰기 | 체크 |

**Run workflow** 를 누르면 목록에 실행이 뜬다. 클릭하면 진행 상황이 실시간으로 보인다.
3~5분이면 끝난다.

각 단계 왼쪽이 전부 초록 체크면 성공이다. 마지막 **데이터 배포** 단계를 펼치면
주소가 찍혀 있다.

## 4단계 · Pages 를 data 브랜치로

3단계가 성공하면 `data` 브랜치가 생겼다. Pages 가 그쪽을 보게 바꾼다.

**Settings** → **Pages**
- Source: `Deploy from a branch`
- Branch: **`data`** / **`/ (root)`** → **Save**

1~2분 뒤 브라우저에서 확인한다.

```
https://mmaximus9981-sudo.github.io/rs-data/latest.json
```

JSON 이 쏟아지면 성공이다.

## 5단계 · 앱에 주소 넣기

`app_template.html` 을 열고 위쪽 이 줄을 찾는다.

```javascript
const SNAPSHOT_URL = "";
```

주소를 넣는다.

```javascript
const SNAPSHOT_URL = "https://mmaximus9981-sudo.github.io/rs-data/latest.json";
```

저장하고 `app.html` 을 다시 만든다.

```
python -c "t=open('app_template.html',encoding='utf-8').read();open('app.html','w',encoding='utf-8').write(t.replace('/*__DATA__*/',open('latest.json',encoding='utf-8').read()))"
```

**이 한 번만 하면 끝이다.** 이후로는 데이터가 바뀌어도 앱을 다시 만들 필요가 없다.
앱이 켜질 때마다 저 주소에서 최신을 받아온다.

## 6단계 · 전체 실행

3단계와 같은 방법으로, 이번엔 제한 없이.

| 항목 | 값 |
|---|---|
| 종목 수 제한 | `0` |
| 실적 수집 건너뛰기 | 체크 해제 |

20~40분 걸린다. 끝나면 앱을 열어 오른쪽 위 기준일을 확인한다.
**날짜만 있으면 성공**이고, `· 내장` 이 붙어 있으면 원격을 못 받은 것이다.

---

## 이제부터

아무것도 안 해도 된다. 한국 시간 **화~토 아침 7시경**에 자동으로 갱신된다.
(미국 장이 월~금 마감하므로 한국에서는 하루 밀린 요일에 뜬다)

시간을 바꾸려면 `.github/workflows/daily-snapshot.yml` 의 이 줄을 고친다.

```yaml
- cron: "0 22 * * 1-5"
```

`분 시 일 월 요일` 이고 **UTC 기준**이다. 한국 시간에서 9를 빼면 된다.
아침 8시에 받고 싶으면 `0 23 * * 1-5`.

---

## 확인과 문제 해결

**돌았는지 어떻게 아나**
Actions 탭에 실행 이력이 쌓인다. 초록 체크면 성공, 빨간 X면 실패다.
실패하면 깃허브가 가입 이메일로 알려준다.

**빨간 X 가 떴다**
실행을 클릭하고 빨간 단계를 펼쳐 로그를 본다. 대부분 둘 중 하나다.

- **야후 레이트 리밋** — 오른쪽 위 **Re-run jobs** 로 다시 돌린다.
  캐시가 남아 있어 두 번째는 빠르다.
- **검증 실패** — `스냅샷 검증` 단계에 이유가 한 줄로 찍힌다.
  이 경우 배포는 되지 않았으니 폰의 앱은 멀쩡하다.

**60일 넘게 커밋이 없으면 예약이 멈춘다**
깃허브 정책이다. 이때는 Actions 탭에서 한 번 손으로 돌리면 다시 살아난다.

**앱에 계속 `· 내장` 이 뜬다**
주소 오타이거나 Pages 브랜치가 아직 `main` 이다. 4단계를 확인한다.
브라우저에서 주소를 직접 열어 JSON 이 보이는지가 가장 빠른 판별법이다.

**무료 한도가 걱정된다**
Actions 탭 → 오른쪽 위 점 세 개 → Usage 에서 이번 달 사용량을 볼 수 있다.
하루 30분 × 22일 = 월 660분이라 2,000분 한도 안이다.
공개 저장소는 아예 무제한이라 사실 신경 쓸 필요가 없다.
